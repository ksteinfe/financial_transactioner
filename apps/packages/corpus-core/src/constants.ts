/** File name for the derived aggregate index at the corpus root (see corpus-format contract). */

export const CORPUS_SUMMARY_FILENAME = 'corpus-summary.json'

/** Bumped when the summary JSON shape changes incompatibly. */

export const CORPUS_SUMMARY_SCHEMA_VERSION = '1.0'
