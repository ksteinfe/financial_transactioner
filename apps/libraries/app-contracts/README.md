# `@txn/app-contracts`

Defines **AppManifest** and **AppCapabilityProfile** (and related IPC contracts as they are added).

Used by `@txn/hello` (and other apps) to fix preload exposure and UI mode labels. See [docs/capability-model.md](../../docs/capability-model.md).
